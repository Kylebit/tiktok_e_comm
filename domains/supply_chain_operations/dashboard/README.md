# 本土备货决策台

这是供应链运营域内的纯本地、只读决策页面。它不连接雅仓、平台或数据库，
也不包含凭据。页面内当前数据是 2026-07-30 的脱敏审计快照。

在本目录运行：

```powershell
C:\Users\Windows11\Desktop\Agent_PR\tiktok_e_comm\.venv\Scripts\python.exe -m http.server 8874
```

然后打开 `http://127.0.0.1:8874/`。

当前 496 件是马来西亚仓库存未知时的备料上限。填写每个 SKU 的可用库存和
高可信在途后，页面会按到仓目标自动下调。冻结、分配、未上架与低可信在途
均不参与扣减。
